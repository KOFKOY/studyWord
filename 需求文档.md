
# 辅助练字网站开发文档（PRD + 技术方案）

## 1. 项目概述

### 1.1 项目目标
开发一个无需登录、打开即用的练字辅助网站。用户输入一个汉字后，页面展示：
- 使用行楷字体渲染的**大字**，居中显示在**九宫格/米字格**中
- 该字的**拼音/发音**
- 该字的**标准笔顺拆解**（逐笔动画演示）
并支持**打印**（生成适合 A4 纸输出的版式），同时适配手机端使用。

### 1.2 核心原则
- 纯前端静态站点：无需后端、无需数据库、无需账号体系
- 快速响应：输入后即时渲染
- 版权合规：字体与笔顺数据需具备可用授权（见第 9 章）

---

## 2. 范围定义

### 2.1 功能范围（包含）
1) 单字输入与渲染  
2) 九宫格/米字格背景显示  
3) 行楷字体大字居中、自动缩放到最大可视尺寸  
4) 拼音显示（含声调）  
5) 标准笔顺拆解（逐笔动画、播放控制）  
6) 打印功能（A4 半页版式）  
7) 移动端适配（响应式布局、触控友好）

### 2.2 非功能范围（不包含）
- 用户登录/注册/云端保存
- 多字排版（字帖批量生成）——后续可扩展
- 识别用户手写并评分（OCR/轨迹评估）——后续可扩展
- “行楷连笔笔顺”数据（本期采用标准笔顺）

---

## 3. 用户使用流程

1) 用户打开网站（首页即功能页）
2) 在输入框输入文字（可粘贴一段文本）
3) 系统自动提取**第一个汉字**作为目标字
4) 页面更新显示：
   - 九宫格中居中的行楷大字
   - 拼音
   - 笔顺动画（默认自动播放一次，可手动控制）
5) 用户点击“打印”按钮，进入浏览器打印对话框，输出 A4 半页练字模板

---

## 4. 详细需求说明（功能需求）

### 4.1 输入模块
- 输入控件：单行输入框（支持粘贴）
- 解析规则：
  - 从输入字符串中提取第一个汉字（Unicode CJK 范围）
  - 若未检测到汉字：提示“请输入汉字”
- 触发方式：
  - 输入变化自动刷新（可加 300ms 防抖）
  - 或点击“生成”按钮刷新（移动端可保留按钮以避免频繁刷新）

### 4.2 九宫格/米字格显示
- 显示区域：正方形画布（Canvas），宽度随屏幕自适应
- 格线要求：
  - 外框（粗线）
  - 九宫格（井字）线
  - 米字格对角线（可选，建议默认开启）
- 视觉要求：
  - 线条颜色浅灰（打印友好）
  - 背景白色

### 4.3 行楷大字渲染
- 字体：使用用户提供字体文件 `AaBiZouLongSheXingKaiTi-2.ttf`
- 渲染方式：Canvas `fillText`（或 SVG text，最终以打印稳定性为准）
- 排版要求：
  - 字居中（水平、垂直）
  - 字号自动计算：在不超出格子边界的前提下尽可能大
  - 字色：黑色（打印友好）
- 字体加载要求：
  - 字体未加载完成前不渲染（避免测量错误导致错位）
  - 字体加载失败时回退到系统楷体（KaiTi）并提示“字体加载失败”

### 4.4 拼音/发音显示
- 显示内容：拼音（带声调），例如 “hǎo”
- 数据来源：前端拼音库（如 pinyin-pro）
- 多音字策略（本期）：
  - 默认使用库的常用读音
  - 可选：提供下拉选择候选读音（增强项，非必须）

### 4.5 标准笔顺拆解（动画）
- 使用 Hanzi Writer 或同类库实现
- 展示内容：
  - 笔画逐笔绘制动画
  - 当前笔画高亮
- 控制按钮（至少包含）：
  - 播放/暂停
  - 重播
  - 下一笔/上一笔（可选但推荐）
- 异常处理：
  - 若该字无笔顺数据：显示“暂无笔顺数据”，但仍显示大字与拼音

### 4.6 打印功能（A4 半页）
- 点击“打印”触发 `window.print()`
- 打印版式要求：
  - A4 纸纵向
  - 内容占用半页高度（约 148mm），居中排版
  - 打印时隐藏输入框、按钮等交互元素，仅保留练字内容
- 打印内容（本期建议）：
  - 九宫格大字
  - 拼音
  - （可选）笔画序号/笔顺静态图：若实现成本高，可先不打印动画，仅屏幕展示动画

> 说明：不同浏览器打印边距不同，需在页面提示用户选择“无页眉页脚/边距最小或默认”。

### 4.7 移动端适配
- 响应式布局：
  - 手机竖屏：九宫格占满宽度，笔顺区域在下方
  - 平板/桌面：九宫格与笔顺区域可左右布局
- 触控友好：
  - 按钮高度 ≥ 40px
  - 输入框自动聚焦可选（避免移动端弹键盘影响布局）

---

## 5. 页面与交互（UI 结构）

### 5.1 页面结构（单页）
- Header（可选）：网站标题“练字助手”
- 输入区：输入框 + 清空按钮 + 打印按钮
- 展示区：
  - 左/上：九宫格 Canvas（大字）
  - 右/下：拼音 + 笔顺动画区域 + 控制按钮
- Footer（可选）：字体/数据来源声明（用于合规）

### 5.2 状态提示
- 空输入：提示“请输入一个汉字”
- 字体加载中：显示 loading（或骨架）
- 笔顺数据缺失：提示“该字暂无笔顺数据”

---

## 6. 技术方案（实现设计）

### 6.1 前端框架与构建
- Vite + Vue 3（Composition API）
- TypeScript（推荐，减少 bug）
- 部署：静态托管（Cloudflare Pages/Vercel/GitHub Pages）

### 6.2 字体集成
- 将 `AaBiZouLongSheXingKaiTi-2.ttf` 放入 `public/fonts/`
- 生产环境建议：
  - 转换为 WOFF2（体积更小）
  - 可做子集化（仅保留常用汉字）以提升加载速度
- CSS：
  - `@font-face` 注册字体名，例如 `BiZouXingKai`

### 6.3 九宫格与大字绘制（Canvas）
- 使用 Canvas 绘制格线与文字
- 适配高清屏：按 `devicePixelRatio` 放大画布
- 字号自适应策略：
  - 以容器内边距为约束
  - 通过测量文字宽度（`measureText`）迭代/二分查找字号
- 字体加载完成后再绘制：
  - `await document.fonts.load('64px BiZouXingKai')`

### 6.4 拼音生成
- 使用 `pinyin-pro`：
  - 输出带声调拼音
  - 仅取目标字

### 6.5 笔顺动画
- 使用 Hanzi Writer：
  - 初始化容器（SVG）
  - 加载对应汉字数据 JSON
  - 提供播放控制 API
- 数据策略：
  - 本地内置常用字数据（推荐）
  - 缺字提示

### 6.6 打印实现
- CSS `@media print`：
  - 隐藏交互控件
  - 固定打印容器尺寸（mm）
- 打印稳定性增强（推荐）：
  - 打印前将 Canvas 导出为 PNG（`canvas.toDataURL()`），打印时用 `<img>` 替代 Canvas，避免部分浏览器打印 Canvas 失真/空白问题

---

## 7. 性能与兼容性要求

### 7.1 性能
- 首屏加载：尽量 < 2s（取决于字体体积）
- 输入响应：< 200ms（不含字体首次加载）
- 字体优化：WOFF2 + 子集化（强烈建议）

### 7.2 兼容性
- 移动端：iOS Safari、Android Chrome
- 桌面端：Chrome、Edge
- 打印：Chrome/Edge 优先保证；Safari 打印差异需测试

---

## 8. 验收标准（可测试条目）

1) 打开网站无需登录即可使用  
2) 输入任意文本，能正确提取第一个汉字并渲染  
3) 九宫格显示正确，线条清晰  
4) 大字使用指定行楷字体渲染，且居中、尽可能大、不溢出  
5) 拼音正确显示（至少能输出一个读音）  
6) 笔顺动画可播放/暂停/重播；无数据时有明确提示  
7) 手机端布局不溢出、按钮可点击、体验正常  
8) 点击打印后，打印预览中只出现练字内容，版式为 A4 半页，内容不被裁切

---

## 9. 资源与版权合规（必须项）

### 9.1 字体文件
- 字体：`AaBiZouLongSheXingKaiTi-2.ttf`
- 字体已授权

### 9.2 笔顺数据
- 使用 Hanzi Writer/Make Me a Hanzi 数据时，需要在项目中保留其许可声明与来源说明（通常是开源许可，按要求保留即可）。

---

## 10. 交付物清单

- 前端项目源码（Vite + Vue3 + TS）
- 字体集成与加载逻辑
- 九宫格 Canvas 渲染模块
- 拼音模块
- 笔顺动画模块（含数据加载策略）
- 打印样式与打印前处理
- README（本开发文档可作为 README 的基础）

---

## 11. 里程碑建议（开发排期参考）

- Day 1：项目脚手架 + 页面布局 + 九宫格 Canvas + 字体加载与大字自适应
- Day 2：拼音 + Hanzi Writer 集成 + 控制按钮
- Day 3：打印版式 + 移动端适配 + 兼容性测试与优化（WOFF2/子集化）

---

## 我需要您补充的两项信息（为了把文档“合规闭环”）
1) 该字体的**授权说明/来源链接**（或您确认“允许商用、网页嵌入、再分发”）  
2) 打印时是否需要把“笔顺步骤”也打印出来？（如果要，我会在需求里明确：打印静态笔顺图/笔画序号的版式与实现方式）

您把字体授权信息发我后，我可以把第 9 章写成可审计版本，并给出下一步的“项目目录结构 + 关键代码实现清单”。